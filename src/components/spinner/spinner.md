```jsx
import { Spinner } from './spinner';

<Spinner />;
```

Different sizes

```jsx padded
import { Spinner } from './spinner';

<>
  <Spinner size={16} />
  <Spinner size={20} />
  <Spinner size={24} />
</>;
```
